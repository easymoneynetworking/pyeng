crypto isakmp policy {{ data.tun_num }}
 encr aes
 authentication pre-share
 group 5
 hash sha

crypto isakmp key cisco address {{ data.wan_ip_2 }}

crypto ipsec transform-set AESSHA esp-aes esp-sha-hmac
 mode transport

crypto ipsec profile GRE
 set transform-set AESSHA

interface Tunnel {{ data.tun_num }}
 ip address {{ data.tun_ip_1 }}
 tunnel source {{ data.wan_ip_1 }}
 tunnel destination {{ data.wan_ip_2 }}
 tunnel protection ipsec profile GRE

